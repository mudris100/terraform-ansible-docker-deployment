[defaults]

# disable host key checking (useful for new EC2 instances)
host_key_checking = False

# default SSH user
remote_user = ubuntu

# private key for EC2 SSH
private_key_file = ~/.ssh/id_ed25519

# faster SSH connections
timeout = 30